# Execution Steps

## 1. Open the project folder

```powershell
cd C:/Users/KIIT0001/Desktop/new paper for nifty/CodexGuardian
```

## 2. Create a virtual environment

```powershell
python -m venv .venv
```

## 3. Activate the environment

```powershell
.venv\Scripts\Activate.ps1
```

## 4. Install dependencies

```powershell
pip install -r requirements.txt
```

## 4.1 Start PostgreSQL and Redis (Docker Desktop)

```powershell
docker compose up -d
```

The root `.env` supplies the development connection variables. Keep it outside `frontend` and `backend`; do not put real production secrets in source control.

## 4.2 Configure the GitHub pull-request agent

1. Create a GitHub App with **Pull requests: Read-only** and **Contents: Read-only** permissions.
2. Set `GITHUB_APP_ID`, `GITHUB_APP_PRIVATE_KEY`, and a strong `GITHUB_WEBHOOK_SECRET` in the root `.env`.
3. Set the GitHub App webhook URL to `https://YOUR_PUBLIC_DOMAIN/api/v1/webhooks/github`, select **Pull requests**, and use the same secret.
4. Restart the backend after changing `.env`.

## 5. Run the backend

```powershell
python -m uvicorn backend.app.main:app --reload --host 0.0.0.0 --port 8000
```

## 6. Open the dashboard

Open the file:

```text
frontend/index.html
```

## 7. Run the test suite

```powershell
pytest -q
```

## 8. Example API calls

### Health check

```powershell
curl http://localhost:8000/health
```

### Review diff

```powershell
curl -X POST http://localhost:8000/api/v1/review/diff -H "Content-Type: application/json" -d "{\"repository_path\":\"C:/path/to/repo\"}"
```

### Build index

```powershell
curl -X POST http://localhost:8000/api/v1/indexing/build -H "Content-Type: application/json" -d "{\"repository_path\":\"C:/path/to/repo\"}"
```

### Retrieve context

```powershell
curl -X POST http://localhost:8000/api/v1/rag/retrieve -H "Content-Type: application/json" -d "{\"repository_path\":\"C:/path/to/repo\",\"query\":\"Python tests\"}"
```

### AI review

```powershell
curl -X POST http://localhost:8000/api/v1/review/ai -H "Content-Type: application/json" -d "{\"repository_path\":\"C:/path/to/repo\"}"
```
<!-- webhook review test -->
